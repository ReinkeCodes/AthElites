/**
 * UI copy constants for consistent messaging across the app.
 * Add new strings here to keep UI text centralized and maintainable.
 */

// Tonnage tracking UI copy
// Used to communicate that tonnage totals only include sessions logged after the feature rollout
export const TONNAGE_ROLLOUT_LABEL = 'Tonnage (tracked since rollout)';
export const TONNAGE_TRACKING_NOTE = 'Totals include workouts logged after tonnage tracking was introduced.';
